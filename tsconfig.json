{
  "compilerOptions": {
    /* Caminhos básicos */
    "baseUrl": "./",
    "paths": {
      "@/*": ["src/*"]
    },

    /* Inclui suas tipagens manuais */
    "typeRoots": [
      "./src/types",
      "./node_modules/@types"
    ],

    /* Modo de módulo e compatibilidade */
    "moduleResolution": "node",
    "module": "esnext",
    "target": "esnext",
    "jsx": "react-jsx",

    /* Regras de segurança e boas práticas */
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

    /* Vite (esbuild) faz a transpilação de verdade — tsc aqui é só type-check */
    "noEmit": true
  },

  /* Inclui todos os arquivos TypeScript da pasta src */
  "include": ["src"],
}
